========================
Command Line Tools Guide
========================

This guide explains the usage of the command line tools provided by Third Origin LLP.

.. contents::

Basic Usage
-----------

You can run the command line tools using:

.. code-block:: bash

   python tool.py [options]

Options
-------

- `-c`, `--count`: Count words in a text file.
- `-f`, `--find`: Find a specific word.
